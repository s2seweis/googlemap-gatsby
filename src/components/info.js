import React from 'react';
// import {title, getImage} from 'gatsby-plugin-image';

const Infotext = (title) => {
console.log(title);

  return (
    <div >

      

      <h1>Content INFO!</h1>

    </div>
  );
};

export default Infotext;
